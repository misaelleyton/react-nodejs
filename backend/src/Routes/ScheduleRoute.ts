import { Router } from 'express'
import * as ScheduleController from '../Controllers/ScheduleController'
const router: Router = Router()

// create Schedule
// router.post("/", ScheduleController.createSchedule);

// //get Schedule
// router.get("/:scheduleId", ScheduleController.getSchedule);

// //get Schedule By user State
// router.get("/:userId", ScheduleController.getScheduleByState);

// //update Schedule
// router.patch("/", ScheduleController.updateSchedule);

// //delete post
// router.delete("/:scheduleId", ScheduleController.deteleSchedule);

export default router
